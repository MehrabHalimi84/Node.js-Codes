callback

کد هایی که طول میکشن رو میزاری تو فانکشن های جدا و با ارگیومند کال بک 
بعدش جایی که واقعا باید کد باشه رو هم همون فانکشن ر. کالل میکنی

function getRepositories(usename, callback) {
    // ......
    // ......
}

 getRepositories(user.gitHubUsername, (repos) => {
       // .....
    })

حالا میشه تو هم تو هم بودنشو اوکی کرد

میشه هر فانکشنی که اسم نداره رو بیاری بیرون یه فانکشن جدا درسستی ازش و بعد فقط جای گذاریش کنی کاللش نکنی

بعد از اون خالت تو هم تو هم در میاد
